# shell feature

Clean Architecture layers:

- `domain`: shell-level route/menu concepts.
- `application`: shell orchestration use cases.
- `interface-adapters`: controllers for user flows.
- `frameworks-drivers`: React components and navigation wiring.
