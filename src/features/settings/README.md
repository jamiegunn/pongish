# settings feature

Clean Architecture layers:

- `domain`: settings value objects and constraints.
- `application`: settings read/write use cases.
- `interface-adapters`: settings mappers/presenters.
- `frameworks-drivers`: browser settings storage adapters.
